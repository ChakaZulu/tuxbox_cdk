diff -Naur openssl-0.9.7.org/Configure openssl-0.9.7/Configure
--- openssl-0.9.7.org/Configure	2002-12-28 02:35:11.000000000 +0100
+++ openssl-0.9.7/Configure	2002-12-31 17:13:04.000000000 +0100
@@ -562,6 +562,9 @@
 ##### OS/2 EMX
 "OS2-EMX", "gcc::::::::",
 
+##### TuxBox
+"tuxbox","powerpc-tuxbox-linux-gnu-gcc:-DB_ENDIAN -DTERMIO -O3 -mcpu=823 -mmultiple -mstring -meabi -msoft-float -fomit-frame-pointer -Wall::-D_REENTRANT:-ldl:BN_LLONG DES_UNROLL DES_RISC2 DES_PTR MD2_CHAR RC4_INDEX::::::::::dlfcn:linux-shared:-fPIC",
+
 ##### VxWorks for various targets
 "vxworks-ppc405","ccppc:-g -msoft-float -mlongcall -DCPU=PPC405 -I\$(WIND_BASE)/target/h:::VXWORKS:-r:::::",
 "vxworks-ppc750","ccppc:-ansi -nostdinc -DPPC750 -D_REENTRANT -fvolatile -fno-builtin -fno-for-scope -fsigned-char -Wall -msoft-float -mlongcall -DCPU=PPC604 -I\$(WIND_BASE)/target/h \$(DEBUG_FLAG):::VXWORKS:-r:::::",
diff -Naur openssl-0.9.7.org/Makefile.org openssl-0.9.7/Makefile.org
--- openssl-0.9.7.org/Makefile.org	2002-12-30 00:03:16.000000000 +0100
+++ openssl-0.9.7/Makefile.org	2002-12-31 17:13:04.000000000 +0100
@@ -271,10 +271,9 @@
 	libs='-L. ${SHLIBDEPS}'; for i in ${SHLIBDIRS}; do \
 	( set -x; ${CC} ${SHARED_LDFLAGS} \
 		-shared -o lib$$i.so.${SHLIB_MAJOR}.${SHLIB_MINOR} \
-		-Wl,-soname=lib$$i.so.${SHLIB_MAJOR}.${SHLIB_MINOR} \
+		-Wl,-soname=lib$$i.so.${SHLIB_MAJOR} \
 		-Wl,-Bsymbolic \
-		-Wl,--whole-archive lib$$i.a \
-		-Wl,--no-whole-archive $$libs ${EX_LIBS} -lc ) || exit 1; \
+		-Wl,--whole-archive lib$$i.a -Wl,--no-whole-archive ) || exit 1; \
 	libs="$$libs -l$$i"; \
 	done
 
@@ -420,6 +419,20 @@
 		done; \
 	fi
 
+install_cdk:
+	@for i in $(EXHEADER) ;\
+	do \
+	(cp $$i $(INSTALL_PREFIX)$(INSTALLTOP)/include/openssl/$$i; \
+	chmod 644 $(INSTALL_PREFIX)$(INSTALLTOP)/include/openssl/$$i ); \
+	done;
+	@for i in $(SHLIBDIRS) ;\
+	do \
+	if [ -d "$$i" ]; then \
+		(cd $$i; echo "installing $$i..."; \
+		$(MAKE) INSTALL_PREFIX='${INSTALL_PREFIX}' INSTALLTOP='${INSTALLTOP}' install ); \
+	fi; \
+	done	
+
 # This assumes that GNU utilities are *not* used
 # HP-UX includes the full pathname of libs we depend on, so we would get
 # ./libcrypto (with ./ as path information) compiled into libssl, hence
